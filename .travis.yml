if: tag IS present
dist: xenial
language: python
python:
- '3.7'
install:
- pip install wheel
script: echo "No build-time tests to run."
deploy:
  provider: pypi
  user: adamrehn
  distributions: bdist_wheel
  on:
    tags: true
